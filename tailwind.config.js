/** @type {import('tailwindcss').Config} */
module.exports = {
  theme: {
    extend: {
      colors: {
        primary: "#68C9FD", // Dark blue
        secondary: "#1E3A8A", // Lighter blue
        accent: "#F43F5E", // Accent color (pink/red)
        customGray: "#F3F4F6", // Custom gray color
      },
    },
  },
  plugins: [],
}

